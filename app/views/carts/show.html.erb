<%
=begin%>
 <p id="notice"><%= notice %></p>

<%= link_to 'Edit', edit_cart_path(@cart) %> |
<%= link_to 'Back', carts_path %>
<%
=end%>
<%=render(@cart.line_items) %>
<%=link_to "empty cart", @cart, method: :delete, data:{confirm: "Are you sure?"}, class:"btn btn-danger ml-5"%>
<br>
<hr>
<h2>Have a coupon?</h2>
<input type=text >
<h4 class="float-right",><%=number_to_currency(@cart.total_price, unit:"PKR ")%></h4>
  <%if user_signed_in? %>
    <%= render "charges/new" %>
    <%else%>

       <%  flash[:notice] = 'You Must log In to Proceed' %>

  <%end%>


<%
=begin%>
 <%= form_tag charges_path do %>
<article>
  <% if flash[:error].present? %>
  <div id="error_explanation">
    <p><%= flash[:error] %></p>
  </div>
  <% end %>

  <label class="amount">
  <span>Amount: PKR<%= @cart.total_price %></span>
  </label>
</article>

<script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
  data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
  data-description="A month's subscription"
  data-amount="<%= @cart.total_price %>"
  data-locale="auto"></script>
  <input type=”hidden” name=”stripeToken” />

<% end %>

<%
=end%>
