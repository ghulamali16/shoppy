



    <%= image_tag(@product.image_url(:default)) %>
    </p>
    <div class="jumbotron", style="width:50rem">
      <h1 class="display-10"><%= @product.title%>  <%=link_to 'Add to Cart', products_path, class:"btn btn-warning mr-1 float-right" %></h1>
      <p class="lead"><%= number_to_currency(@product.price, unit: "PKR ", precision:0) %></p>
      <p class="lead"><%= @product.description %></p>
      <p class="lead"><%= @product.make %></p>
      <hr class="my-4">
      <p>
      <strong>Sold By:</strong>
      <%= @product.user.name %>
      </p>
      <%if product_owner(@product)%>
      <%= link_to 'Edit', edit_product_path(@product), class:"btn btn-warning mr-3" %>
      <%= link_to 'Delete', @product,method: :delete, data: {confirm: "Are your sure?"} , class:"btn mr-3 btn-danger" %>
      <%end%>
    <%= link_to 'Back', products_path , class:"btn btn-dark" %>


    </div>











<%
=begin%>
 <div class="jumbotron">
<h2><%= @product.title %>   <%= link_to 'Add to cart', products_path , class:"btn btn-warning float-right " %></h2>
</p>
<p>
  <strong>Price:</strong>
  <%= @product.price %>



<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>



<p>
  <strong>Make:</strong>
  <%= @product.make %>
</p>
<p>
  <strong>Sold By:</strong>
  <%= @product.user.name %>
</p>
</div>

 <%if product_owner(@product)%>
   <%= link_to 'Edit', edit_product_path(@product), class:"btn btn-warning mr-3" %>
   <%= link_to 'Delete', @product,method: :delete, data: {confirm: "Are your sure?"} , class:"btn mr-3 btn-danger" %>
  <%end%>
<%= link_to 'Back', products_path , class:"btn btn-dark" %>
<%
=end%>
